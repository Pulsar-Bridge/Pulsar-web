# Soroban contract (client-exposed — read-only identifiers only)
NEXT_PUBLIC_CONTRACT_ID=
NEXT_PUBLIC_SOROBAN_RPC_URL=https://soroban-testnet.stellar.org
NEXT_PUBLIC_STELLAR_NETWORK_PASSPHRASE=Test SDF Network ; September 2015

# pulsar-core relay API (server-only — never prefix these NEXT_PUBLIC_)
RELAY_API_BASE_URL=
RELAY_API_KEY=
RELAY_ADMIN_API_KEY=
